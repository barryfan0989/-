# 專題：台灣售票網站資料整合與爬蟲

## 專案背景與目標
本專案使用 Python 建構一套爬取並整合台灣主要售票平台（針對「表演/演唱會」類別）的資料管線，透過資料清洗與資料增強（如維基百科）補齊藝人與場館資訊，最後將結構化資料存入 MySQL 以供前端或分析使用。

## 技術與工具

- 語言：Python 3.x
- 爬蟲核心：Scrapling（https://github.com/D4Vinci/Scrapling）
- 資料庫：MySQL（存取可使用 `pymysql` 或 `SQLAlchemy`）
- 架構：物件導向 (OOP)、策略模式 (Strategy Pattern)、模組化設計

## 主要輸出

- 七個實體表（`users`, `artists`, `artist_news`, `artist_categories`, `venues`, `events`, `event_tickets`）與一個多對多橋樑表 `event_artists`。
- 一個檢視表 `v_all_events_summary`，以便前端一次取得完整活動資訊。

---

## MySQL Schema（概要）

以下為欄位設計摘要，實作時請以自動建表腳本（SQL 或 SQLAlchemy）撰寫：

- `users`
	- `id` INT PRIMARY KEY AUTO_INCREMENT
	- `email` VARCHAR UNIQUE
	- `password_hash` VARCHAR
	- `registered_at` DATETIME

- `artists`
	- `id` INT PRIMARY KEY AUTO_INCREMENT
	- `name` VARCHAR UNIQUE
	- `wiki_intro` TEXT NULL
	- `wiki_url` VARCHAR NULL

- `artist_news`
	- `id` INT PRIMARY KEY AUTO_INCREMENT
	- `artist_id` INT REFERENCES `artists(id)`
	- `title` VARCHAR
	- `url` VARCHAR
	- `published_at` DATETIME NULL

- `artist_categories`
	- `id` INT PRIMARY KEY AUTO_INCREMENT
	- `artist_id` INT REFERENCES `artists(id)`
	- `genre` VARCHAR NULL
	- `language` VARCHAR NULL

- `venues`
	- `id` INT PRIMARY KEY AUTO_INCREMENT
	- `name` VARCHAR UNIQUE
	- `address` VARCHAR NULL
	- `city` VARCHAR NULL
	- `description` TEXT NULL

- `events`
	- `id` INT PRIMARY KEY AUTO_INCREMENT
	- `venue_id` INT REFERENCES `venues(id)` NULLABLE
	- `name` VARCHAR
	- `source_site` VARCHAR
	- `ticket_sale_time` DATETIME NULL
	- `event_time` DATETIME NULL
	- `event_url` VARCHAR
	- `scraped_at` DATETIME

- `event_tickets`
	- `id` INT PRIMARY KEY AUTO_INCREMENT
	- `event_id` INT REFERENCES `events(id)`
	- `ticket_type` VARCHAR
	- `price` INT

- `event_artists`（多對多橋樑表）
	- `event_id` INT
	- `artist_id` INT
	- PRIMARY KEY(`event_id`, `artist_id`)

- `v_all_events_summary`（VIEW）
	- 使用 LEFT JOIN 串接 `events`, `venues`, `event_tickets`, `event_artists`, `artists`, `artist_categories`, `artist_news`（不含 `users`），提供一筆整合化的活動摘要。

---
## mysql帳號密碼
Service URI：
mysql://
CLICK_TO:REVEAL_PASSWORD
@ticketdb-ticket63.f.aivencloud.com:13599/defaultdb?ssl-mode=REQUIRED
Database name：defaultdb
Host：ticketdb-ticket63.f.aivencloud.com
Port：13599
User：avnadmin
Password：AVNS_QqNVFqacdQinAgGmXY9
SSL mode：REQUIRED
## 系統模組設計（高階）

### 模組一：售票網爬蟲（Stage 1 Crawler）


- 目標網站：Indievox、KKTIX、ticket.com.tw（拓元）、ibon、寬宏（Kham）。
- 條件：只抓取「表演/演唱會」活動，排除展覽與體育。
- 欄位：活動名稱、來源網站、活動網址、演出時間、購票時間、場館名稱、藝人名稱、票種與票價等。
- 實作：定義 `BaseTicketCrawler` 抽象類，各站實作自己的 `Crawler` 並回傳統一的資料結構（`Event` dataclass）。

**實作位置**：`src/crawlers/`
- `data_models.py` - 資料模型（`Event`, `Ticket`, `CrawlResult`）
- `base_crawler.py` - 基礎爬蟲抽象類
- `stage1_crawlers.py` - 5 個具體爬蟲實現
- `crawler_manager.py` - 爬蟲工廠與管理器
- `README.md` - 詳細文件與範例

**關鍵特性**：
- Strategy Pattern 設計，易於擴展新站點
- 統一的資料結構與錯誤處理
- 自動去重與資料驗證
- 完整的日誌記錄

### 模組二：資料增強爬蟲（Stage 2 Crawler）

- 任務：接收 Stage 1 的 `artist_name` 與 `venue_name`，向維基百科或搜尋引擎取得補充資訊（wiki 介紹、wiki 連結、場館地址與城市、藝人新聞與分類等）。
- 防呆：查無結果時回傳 NULL，不中斷整體流程。

### 模組三：資料管線與持久化（Data Pipeline）

- 任務：清洗字符串、格式化日期（`YYYY-MM-DD HH:MM:SS`）、正規化欄位，再按順序寫入資料庫（查詢或建立 venues → events → event_tickets → artists → event_artists → artist_categories / artist_news）。

---

## 開發與實作進度



1. 建立專案骨架：`src/crawlers/`、`scripts/`、`requirements.txt`、`README.md`
2. 撰寫自動建表腳本 (`scripts/setup_clean_database.py`)，已在遠端 MySQL (Aiven) 成功建立 8 個表 + 1 個 VIEW
3. 實作 `BaseTicketCrawler` 抽象類與 5 個具體爬蟲（KKTIX、Indievox、ticket.com.tw、ibon、Kham）
4. 建立爬蟲工廠 (`CrawlerFactory`) 與管理器 (`CrawlerManager`)



4. 實作 Stage 2 的維基查詢模組（含錯誤處理與 rate-limit 處理）
5. 建立 ETL pipeline 實作與測試
6. 撰寫簡單的 CLI 或 scheduler（cron / Airflow / dagster 可選）

---

## 範例：自動建表 SQL 範例（簡化）

```sql
CREATE TABLE artists (
	id INT AUTO_INCREMENT PRIMARY KEY,
	name VARCHAR(255) NOT NULL UNIQUE,
	wiki_intro TEXT,
	wiki_url VARCHAR(512)
);
-- 其他表類似實作
```

---

